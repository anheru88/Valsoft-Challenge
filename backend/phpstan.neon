includes:
    - vendor/larastan/larastan/extension.neon

parameters:
    level: 8
    # Production code only. Pest binds `$this` to the TestCase at runtime, which
    # no static analyser can follow, so the suite is verified by running it.
    paths:
        - app
        - database
        - routes
